{
  "name": "devarena",
  "version": "1.0.0",
  "description": "DevArena - Developer Competition Platform",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd backend && npm install --production=false && cd ../frontend && npm install --production=false && npm run build",
    "start": "cd backend && node src/server.js",
    "start:prod": "cd backend && NODE_ENV=production node src/server.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "competitions",
    "developers",
    "platform"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "jest": "^30.3.0"
  }
}
