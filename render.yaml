services:
  - type: web
    name: devarena
    env: node
    buildCommand: npm run install:all && cd frontend && npm run build
    startCommand: cd backend && node scripts/startup.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
